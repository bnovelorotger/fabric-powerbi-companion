# Configuration

Use `workflow.example.yaml` as the base template for local configuration.

Recommended practice:

- copy it to `workflow.local.yaml`
- fill only local values
- do not commit the local file

Typical local values:

- Fabric workspace and semantic-model names
- preferred lakehouse / warehouse names
- local owner metadata
- private workspace root path
